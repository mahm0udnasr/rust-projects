- to run any projects in this repository, you need to have Rust on your machine.

- can run project with this commond:

```bash
 rustc main.rs
  ./main
```

- to run cargo project, use this command:

```bash
 cargo run
```

- to build cargo project, use this command:

```bash
 cargo build
```